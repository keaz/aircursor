# Xcode project is generated by XcodeGen — never commit it
*.xcodeproj

# Build products & caches
DerivedData/
.build/
.swiftpm/
build/

# User state
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# Raw gesture videos stay local — the repo carries landmark JSON only
docs/gesture recording/
